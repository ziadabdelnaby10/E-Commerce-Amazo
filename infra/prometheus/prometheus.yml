global:
  scrape_interval: 5s # High frequency for fast-acting circuit breakers

scrape_configs:
  - job_name: 'resilience4j-app'
    metrics_path: '/actuator/prometheus'
    static_configs:
      - targets: ['host.docker.internal:8080'] # Use host.docker.internal to target your local machine IDE/App
